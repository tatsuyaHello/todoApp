<% if task.errors.any? %>
	<div id="error_explanation">
		<h2><%= pluralize(task.errors.count, "error") %> prohibited this book from 
		being saved:</h2>

		<ul>
		<% task.errors.full_messages.each do |message| %>
			<li><%= message %></li>
		<% end %>
		</ul>
	</div>
<% end %>
<%= form_for(task) do |f| %>
	<div class="field">
		<p>
		<%= f.label :ToDo%>
		<%= f.text_field :content, size: 25 %>
		</p>
		<p>
		<%= f.label :Deadline%>
		<%= f.date_field :deadline %> 
		</p>
		<p>
		<%= f.label :Finish?%>
		<%= f.check_box :done, {class: 'cnk'} %>
		</p>
		<p>
		<%= f.label :コメント%>
		<%= f.text_field :comment, size: 25 %>
		</p>

		<%= f.submit %>
	</div>
<% end %>